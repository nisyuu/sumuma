# テスト戦略

このドキュメントは、sumumaプロジェクトのテスト戦略を概説するものです。目標は、コードの品質を確保し、リグレッション（機能低下）を防ぎ、アプリケーションの安定性に対する高い信頼性を維持することです。

## 1. テストフレームワーク

Pythonの`unittest`モジュールをベースにした、Djangoの組み込みテストフレームワークを使用します。これには、Djangoアプリケーションをテストするための堅牢なツールセットが含まれています。

-   テストランナー (`manage.py test`)
-   HTTPリクエストをシミュレートするためのテストクライアント (`django.test.Client`)
-   テストを分離するためにデータベーストランザクション管理を提供する`django.test.TestCase`
-   ビューの単体テスト用にリクエストオブジェクトを生成する`django.test.RequestFactory`

## 2. テストの種類

包括的なカバレッジを確保するために、各アプリケーションで以下の種類のテストを実装します。

### a. モデルテスト

各アプリケーションの`models.py`内のモデルについて：

-   **フィールド検証**: `max_length`, `unique`, `null`, `blank`などの制約を検証します。
-   **カスタムメソッド**: モデルに定義されたカスタムメソッドやプロパティをテストします。
-   **文字列表現**: `__str__`メソッドが人間が読める文字列を返すことを確認します。
-   **デフォルト値**: 作成時にフィールドのデフォルト値が正しく設定されることを確認します。

### b. ビューテスト（DRF APIビューを含む）

各アプリケーションの`views.py`内のビューについて：

-   **アクセス制御**:
    -   認証が必要なビューが、未認証のユーザーをログインページにリダイレクトすることを確認します。
    -   特定の権限（例: `is_staff`）を必要とするビューが、アクセスを正しく許可または拒否することを確認します。
-   **HTTPメソッド**:
    -   ビューが許可されたHTTPメソッド（`GET`, `POST`など）に正しく応答することを確認します。
    -   許可されていないメソッドに対して、適切なエラー（例: 405 Method Not Allowed）を返すことを確認します。
-   **テンプレートとコンテキスト（非APIビューの場合）**:
    -   ビューが正しいテンプレートをレンダリングすることを確認します。
    -   テンプレートに渡されるコンテキストデータが正確で完全であることを確認します。
-   **フォームハンドリング**:
    -   成功するフォーム送信（オブジェクトの作成/更新など）をテストし、その結果（データベースの変更、リダイレクト）を検証します。
    -   無効なフォーム送信をテストし、フォームエラーが正しく表示されることを確認します。
-   **APIエンドポイントテスト（DRFの場合）**:
    -   **ステータスコード**: 正しいHTTPステータスコード（200, 201, 400, 401, 403, 404）をテストします。
    -   **ペイロード検証**: 有効および無効なリクエストペイロードをテストし、シリアライザーが期待通りに機能することを確認します。
    -   **認証と権限**: APIエンドポイントを保護し、アクセスルールをテストします。
    -   **シリアライゼーション**: JSONレスポンスの構造と内容が正しいことを検証します。
    -   **フィルタリング/ページネーション**: 該当する場合、これらの機能が正しく機能することをテストします。

### c. フォームテスト

`forms.py`内の各フォームについて：

-   **検証**: フィールド固有の検証や`clean()`メソッドを含め、ビューとは独立してフォームの検証ロジックをテストします。
-   **フィールドレンダリング**: カスタマイズされている場合、フィールドが正しいウィジェットでレンダリングされることを確認します。

### d. URLテスト

-   `django.urls.resolve`と`reverse`を使用して、URL名が意図したビュー関数/クラスに正しくマッピングされることを確認します。これにより、`urls.py`での破壊的な変更を検出します。

## 3. テストの構成

コードベースを整理し、保守性を高めるために、テストは以下のように構成します。

-   各Djangoアプリケーションは`tests/`ディレクトリを持つべきです。
-   `tests/`ディレクトリ内で、テストを論理的なファイルに分割します。
    -   `__init__.py`
    -   `test_models.py`
    -   `test_views.py`
    -   `test_forms.py`
    -   `test_urls.py`

この構造により、アプリケーションの特定の部分に関連するテストを簡単に見つけることができます。

## 4. テストの実行

テストは`manage.py`コマンドを使用して実行できます。

```bash
# プロジェクト内のすべてのテストを実行
poetry run python manage.py test

# 特定のアプリケーションのテストを実行 (例: kakeibo)
poetry run python manage.py test kakeibo

# 特定のテストクラスを実行
poetry run python manage.py test kakeibo.tests.test_views.KakeiboViewTests

# 特定のテストメソッドを実行
poetry run python manage.py test kakeibo.tests.test_views.KakeiboViewTests.test_top_view_authenticated
```

## 5. コードカバレッジ

テストの有効性を測定するために、`coverage`パッケージを使用します。

### セットアップ

```bash
poetry add --group dev "coverage[toml]"
```

### カバレッジの実行

1.  `coverage`を使用してテストを実行します。
    ```bash
    poetry run coverage run manage.py test
    ```

2.  ターミナルでレポートを生成します。
    ```bash
    poetry run coverage report -m
    ```

3.  閲覧用に詳細なHTMLレポートを生成します。
    ```bash
    poetry run coverage html
    ```
    （ブラウザで`htmlcov/index.html`を開いてレポートを表示します）

目標は、テストされていないコードを特定し、徐々にカバレッジを向上させることです。

## 6. 継続的インテグレーション（CI）

テストスイートは、CIパイプライン（GitHub Actions）に統合する必要があります。`main`ブランチへのすべてのプッシュおよびプルリクエストでテストを自動的に実行する新しいワークフローを作成します。これにより、既存の機能を破壊するコードのマージを防ぎます。
